
# 基于次数和设备的收费方案设计

## 设计原则

1. **从时间控制转向次数控制**：更加精确和公平的计费方式
2. **设备作为核心维度**：多设备使用需要更高套餐
3. **功能模块分离计费**：不同功能使用不同的次数配额
4. **灵活的套餐设计**：满足不同用户群体的需求

---

## 新的收费模型

### 核心变化

| 维度 | 旧方案 | 新方案 | 说明 |
|------|--------|--------|------|
| 养号控制 | 每日时长 | 每日次数 | 按养号启动次数计费 |
| 创作控制 | 月度次数 | 每日次数 | 更精细的创作控制 |
| 导出控制 | 月度次数 | 每日次数 | 更精细的导出控制 |
| 设备控制 | 最大设备数 | 最大设备数 | 保持不变，作为核心维度 |

---

## 套餐详细设计

### 免费版 (Free)
**目标用户**: 新用户体验、个人轻度使用

| 功能项 | 配额 | 说明 |
|--------|------|------|
| 最大设备数 | 1 台 | 支持多设备，但是每天只能养号一个设备 |
| 每日养号次数 | 3 次 | 每次启动算1次 ，每次养号时间不超过 20 分钟|
| 每日创作次数 | 20 次 | 每天把 pdf 转成图片的次数，每个 pdf 算一次|
| 每日导出次数 | 20 次 | 每次导出算1次 |
| 单次养号时长 | 20 分钟 | 单次最长运行时间 |
| 功能限制 | 基础功能 | 不支持高级配置 |
主图次数、封面次数 也要限制

**价格**: 免费

---

### 基础版 (Basic)
**目标用户**: 个人重度使用、小型工作室

| 功能项 | 配额 | 说明 |
|--------|------|------|
| 最大设备数 | 2 台 | 支持双设备同时使用 |
| 每日养号次数 | 10 次 | 每次启动算1次 |
| 每日创作次数 | 20 次 | 每次创作算1次 |
| 每日导出次数 | 20 次 | 每次导出算1次 |
| 单次养号时长 | 60 分钟 | 单次最长运行时间 |
| 功能限制 | 完整功能 | 支持所有高级配置 |

**价格**: ¥29/月 或 ¥299/年

---

### 专业版 (Professional)
**目标用户**: 专业用户、中小型团队

| 功能项 | 配额 | 说明 |
|--------|------|------|
| 最大设备数 | 5 台 | 支持多设备同时使用 |
| 每日养号次数 | 50 次 | 每次启动算1次 |
| 每日创作次数 | 100 次 | 每次创作算1次 |
| 每日导出次数 | 100 次 | 每次导出算1次 |
| 单次养号时长 | 120 分钟 | 单次最长运行时间 |
| 功能限制 | 完整功能 + 优先支持 | 专属客服支持 |

**价格**: ¥99/月 或 ¥999/年

---

### 企业版 (Enterprise)
**目标用户**: 大型团队、企业客户

| 功能项 | 配额 | 说明 |
|--------|------|------|
| 最大设备数 | 不限制 | 根据需求定制 |
| 每日养号次数 | 不限制 | 根据需求定制 |
| 每日创作次数 | 不限制 | 根据需求定制 |
| 每日导出次数 | 不限制 | 根据需求定制 |
| 单次养号时长 | 不限制 | 无时长限制 |
| 功能限制 | 完整功能 + 定制开发 + 专属支持 | 一对一技术支持 |

**价格**: 定制报价

---

## 次数统计规则

### 1. 养号次数统计
- **统计时机**: 每次成功启动养号时计数+1
- **不计费场景**: 
  - 养号启动失败（如设备连接失败）
  - 养号在1分钟内主动停止
- **重置周期**: 每日 00:00 重置

### 2. 创作次数统计
- **统计时机**: 每次成功创建笔记时计数+1
- **不计费场景**:
  - 创作保存失败
  - 创作草稿未发布
- **重置周期**: 每日 00:00 重置

### 3. 导出次数统计
- **统计时机**: 每次成功导出内容时计数+1
- **不计费场景**:
  - 导出失败
  - 重复导出同一内容（5分钟内）
- **重置周期**: 每日 00:00 重置

---

## 数据库设计变更

### 新增表：daily_usage_count（每日使用次数统计表）

```sql
CREATE TABLE daily_usage_count (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    device_id TEXT NOT NULL,
    use_date TEXT NOT NULL,
    yanghao_count INTEGER DEFAULT 0,
    create_count INTEGER DEFAULT 0,
    export_count INTEGER DEFAULT 0,
    update_time TEXT NOT NULL,
    UNIQUE(device_id, use_date)
);
```

### 变更表：user_license（用户授权表）

```sql
-- 新增字段
ALTER TABLE user_license ADD COLUMN max_daily_yanghao INTEGER DEFAULT -1;
ALTER TABLE user_license ADD COLUMN max_daily_create INTEGER DEFAULT -1;
ALTER TABLE user_license ADD COLUMN max_daily_export INTEGER DEFAULT -1;
ALTER TABLE user_license ADD COLUMN max_single_yanghao_minutes INTEGER DEFAULT -1;
```

---

## API 接口变更

### 1. 授权信息接口变更

**响应数据新增字段**:
```json
{
  "data": {
    "max_daily_yanghao": 10,
    "max_daily_create": 20,
    "max_daily_export": 20,
    "max_single_yanghao_minutes": 60,
    "used_daily_yanghao": 5,
    "used_daily_create": 8,
    "used_daily_export": 3
  }
}
```

### 2. 启动养号接口变更

**请求前检查**:
- 检查今日养号次数是否超限
- 检查设备数是否超限

**响应新增字段**:
```json
{
  "remaining_yanghao_count": 5,
  "max_single_duration": 60
}
```

---

## 套餐配额对照表

| 套餐 | 设备数 | 每日养号 | 每日创作 | 每日导出 | 单次时长 | 价格(月) | 价格(年) |
|------|--------|----------|----------|----------|----------|-----------|-----------|
| Free | 1 | 3 | 3 | 3 | 30min | 免费 | 免费 |
| Basic | 2 | 10 | 20 | 20 | 60min | ¥29 | ¥299 |
| Professional | 5 | 50 | 100 | 100 | 120min | ¥99 | ¥999 |
| Enterprise | 定制 | 定制 | 定制 | 定制 | 不限 | 定制 | 定制 |

---

## 迁移策略

### 1. 数据迁移
- 保留原有的时间统计数据（用于参考）
- 新增次数统计表
- 平滑过渡，不影响现有用户

### 2. 用户通知
- 提前通知用户收费模式变更
- 提供过渡期（如1个月）
- 现有用户可选择继续使用旧方案或迁移到新方案

### 3. 兼容性处理
- 服务端同时支持两种验证逻辑
- 客户端根据返回数据自动适配
- 渐进式升级，确保系统稳定

---

## 优势分析

### 相比旧方案的优势

1. **更精确的计费**: 按实际使用次数计费，更加公平
2. **更好的用户体验**: 用户可以精确控制使用量
3. **更灵活的套餐**: 满足不同层次用户需求
4. **更低的入门门槛**: 免费版提供基础次数，降低试用成本
5. **更高的收入潜力**: 专业版和企业版提供更高的价值

### 对用户的好处

- 轻量用户：免费版够用，成本更低
- 中度用户：基础版性价比高
- 重度用户：专业版功能完整
- 团队用户：企业版可定制

